# 教学文档网站 - 使用指南

## 🚀 快速启动

### 启动本地预览
```bash
cd D:\teaching-docs
npm run dev
```
然后在浏览器打开 http://localhost:5173

### 构建发布版本
```bash
npm run build
```

---

## 📁 项目结构

```
D:\teaching-docs\
├── docs\                        # 所有内容都在这里
│   ├── index.md                 # 首页
│   ├── guide\                   # 文档教程
│   │   ├── index.md             # 教程首页
│   │   ├── lesson-1.md          # 第一课
│   │   └── lesson-2.md          # 第二课
│   ├── videos\                  # 视频课程
│   │   ├── index.md             # 视频列表
│   │   ├── video-1.md           # 第一讲
│   │   └── video-2.md           # 第二讲
│   ├── public\                  # 静态资源
│   │   └── videos\              # ⬅️ 把视频文件放这里!
│   └── .vitepress\
│       └── config.mts           # 网站配置
└── package.json
```

---

## 📝 如何添加内容

### 添加新文档
1. 在 `docs/guide/` 文件夹新建 `.md` 文件
2. 编辑 `docs/.vitepress/config.mts`，在 sidebar 中添加链接

### 添加新视频
1. 把视频文件（.mp4）放入 `docs/public/videos/` 文件夹
2. 在 `docs/videos/` 新建 `.md` 文件
3. 使用以下代码嵌入视频：

```html
<video controls width="100%">
  <source src="/videos/你的视频名.mp4" type="video/mp4">
</video>
```

---

## 🌐 部署到网上

### 方式一：GitHub Pages（推荐）

1. 在 GitHub 创建新仓库
2. 上传项目代码
3. 启用 GitHub Pages，选择 GitHub Actions
4. 创建 `.github/workflows/deploy.yml` 文件（见下方）

### 方式二：Vercel

1. 注册 Vercel 账号
2. 导入 GitHub 仓库
3. 自动部署完成

---

## ❓ 常见问题

### Q: 视频无法播放？
A: 确保视频放在 `docs/public/videos/` 文件夹，且路径以 `/videos/` 开头

### Q: 如何修改网站标题？
A: 编辑 `docs/.vitepress/config.mts` 中的 `title` 字段

### Q: 如何添加导航菜单？
A: 编辑 `docs/.vitepress/config.mts` 中的 `nav` 数组
